<nav class="bg-white shadow-md fixed top-0 w-full">
  <div class="max-w-7xl mx-auto px-4 py-4 flex items-center justify-between">
    <a href="/" class="text-2xl font-bold text-blue-600"><%= currentPage %></a>
    <div class="space-x-2">
      <% if(isLoggedIn){ %>
      <a href="/home-list"
        class="px-3 py-2 rounded font-medium <%= currentPage === 'Home List' ? 'bg-blue-100 text-blue-700' : 'text-gray-700 hover:bg-blue-100 ' %>">
        Home List
      </a>
      <a href="/host/home-add"
        class="px-3 py-2 rounded font-medium <%= currentPage === 'Home Registration' ? 'bg-blue-100 text-blue-700' : 'text-gray-700 hover:bg-blue-100 ' %>">
        Add Home
      </a>
      <a href="/host/home-list"
        class="px-3 py-2 rounded font-medium <%= currentPage === 'Host Home' ? 'bg-blue-100 text-blue-700' : 'text-gray-700 hover:bg-blue-100 ' %>">
        Host Home
      </a>
      <a href="/home-booking"
        class="px-3 py-2 rounded font-medium <%= currentPage === 'Booking' ? 'bg-blue-100 text-blue-700' : 'text-gray-700 hover:bg-blue-100 ' %>">
        Booking
      </a>

      <a href="/favourites"
        class="px-3 py-2 rounded font-medium <%= currentPage === 'Favourite List' ? 'bg-blue-100 text-blue-700' : 'text-gray-700 hover:bg-blue-100 ' %>">
        Favourite
      </a>

      <% } %>
      <a href="/contact-us"
        class="px-3 py-2 rounded font-medium <%= currentPage === 'Contact Us' ? 'bg-blue-100 text-blue-700' : 'text-gray-700 hover:bg-blue-100 ' %>">
        Contact
      </a>
      <% if(!isLoggedIn){ %>
      <a href="/login"
        class="px-3 py-2 rounded font-medium <%= currentPage === 'Login' ? 'bg-blue-100 text-blue-700' : 'text-gray-700 hover:bg-blue-100 ' %>">
        Login
      </a>
      <% }else{ %>
        <form action="/logout" method="post" class="inline">
          <button type="submit" class="px-3 py-2 rounded font-medium hover:bg-blue-100">LogOut</button>
        </form>
      <% } %>
    </div>
  </div>
</nav>